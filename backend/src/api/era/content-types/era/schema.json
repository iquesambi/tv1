{
  "kind": "collectionType",
  "collectionName": "eras",
  "info": {
    "singularName": "era",
    "pluralName": "eras",
    "displayName": "Temas (40 anos)"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "nome": {
      "type": "string",
      "required": true
    },
    "ordem": {
      "type": "integer",
      "description": "Ordem em que o tema aparece na timeline de 40 anos (menor primeiro)."
    },
    "cases_quarenta_anos": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::case-quarenta-anos.case-quarenta-anos",
      "mappedBy": "tema"
    }
  }
}
